# 網站改建規格 ── 滷肉飯 → 辦桌《阿桃師的辦桌》
### 給 Claude Code 執行｜對既有 repo（grandma-three-rho）操作

---

## 0. 這份檔怎麼用（老闆先做一件事）

> **已知 repo 結構**（依截圖 `grandma-storybook-main`）：Next.js **App Router**（`app/`）＋ **TypeScript** ＋ Tailwind，**靜態匯出**（`out/`）。根目錄有：`CLAUDE.md`、`AGENTS.md`、`public/`、`.git`、`package.json`，以及 `.next`/`out` 建置產物。（舊的 `STORY_CANON.md` 滷肉飯版已刪除；先前的 `期末成品_辦桌版` 資料夾也已移出 repo，皆無干擾。）

1. 把 **`STORY_CANON_C_辦桌_v2.md`** 與這份 **`網站改建規格.md`** 複製進 repo **根目錄**（repo 沒有 docs 資料夾，放根目錄即可）。
2. 開 Claude Code，指向 `grandma-storybook-main` 資料夾，丟給它這句話：
   > 「請先讀根目錄的 `STORY_CANON_C_辦桌_v2.md`（內容權威）與 `網站改建規格.md`（執行規格）。然後把網站從滷肉飯重構成辦桌版《阿桃師的辦桌》。**先盤點現有結構（路由、story 資料檔位置、圖檔資料夾、audio 機制）並回報改動計畫**，做架構與資料層，做完停下來給我看，再做活動。**動工前先 `git tag midterm-backup` 把現在這版標起來（保險）；之後可直接在 main 上做。**」

> **權威順序**：canon v2 是內容真理（劇情、角色、活動正解、能力卡、菜單、鐵則）；本規格是「網站長怎樣、怎麼蓋」。衝突時內容以 canon v2 為準、結構以本規格為準。

---

## 1. 一句話目標

把現有 Next.js 繪本網站，從《阿嬤的料理日記：滷肉飯》重構為 **《阿桃師的辦桌》辦桌版**，並把「故事」與「互動活動」**拆成兩個區塊**（現在是混在一起的）。

---

## 2. 必須命中的硬性要求（老師檢核表，不可省）

- 繪本 **≥30 頁**（本版 32 頁）＋ 可公開的**網址**。
- **≥2 個**「認知＋運算思維融入學習目標」的**數位互動活動**。
- 其中**≥1 個運算思維(CT) 活動**、**≥1 個認知活動**，要能現場 Demo 操作。
- **故事與活動分離**：繪本只講故事；活動放在獨立區塊（canon 鐵則 b6）。
- **長者友善 UX**：大字、左右翻頁不捲動、對話框放留白處不擋臉、活動可重試、回饋用桃姨口氣、每個活動後出「能力卡」。

> 註：canon 設計了 9 個活動，但兩天內**不必全部做成數位**。靜態（繪本內）覆蓋一部分、數位（網頁）覆蓋一部分即可滿足自評表「靜態+數位」雙欄。數位活動的建置優先序見 §5。

---

## 3. 目標架構

兩個頂層區塊，首頁讓使用者選：

```
/                ← 首頁：兩個大按鈕「看繪本」「玩活動」＋書名《阿桃師的辦桌》
/book            ← 繪本：32 頁，左右翻頁，純故事（圖＋旁白＋對話框＋可選朗讀）
/book/[page]     ← 單頁（01–32）
/activities      ← 活動列表（卡片式，標明練的是哪個 CT/認知能力）
/activities/[id] ← 單一活動 ＋ 完成後的「能力卡」
```

- 繪本頁面**不內嵌**活動本體；但在有對應活動的頁（canon 標〔活動X〕者），底部放一顆**選配**按鈕「▶ 來試這頁的活動」深連到 `/activities/[id]`。這樣故事頁保持乾淨（解評語 B/D），又保留教學連動。
- 保留現有的**翻頁導覽**與**朗讀(audio)** 能力；只是換內容。
- 沿用現有 `story.ts`（或等價資料檔）的結構，**改成辦桌資料**；canon §6（逐頁）與 §7（活動正解）可直接灌。

---

## 4. 繪本區塊（/book）規格

- **32 頁**，內容逐頁照 canon §6。每頁資料物件含：
  ```ts
  { page: number,
    image: string,        // 例 "/story/pages/p07.png"（見 §7 命名）
    narration: string,    // 旁白（台語碎念，照 canon 原文）
    dialogues?: {speaker, text, pos}[],  // 對話框，pos 指定放畫面留白處
    activityId?: string,  // 若該頁有對應活動，填 id，底部出「試這頁活動」鈕
    audio?: string }      // 朗讀檔（可後補；先讓欄位在）
  ```
- **翻頁**：左右箭頭 + 鍵盤左右 + 觸控滑動；**整頁切換、不垂直捲動**。頁碼「7 / 32」大字顯示。
- **對話框**：高對比底、圓角、字大（見 §6）；用 `pos` 放在 canon 指定的留白處，**不得壓到人臉或食物主體**。
- **P.3 / P.24 紅單機關**：P.3 紅單「辦給誰」欄位是空白；P.24 觸發翻面動畫，空白欄翻成「謝桃姨」。做一個簡單的 CSS flip 即可，是全書最大的轉，值得花一點點巧思。
- **P.17、P.22 食物特寫頁**：版面讓圖滿版、文字最少（這幾張圖走擬真，見生圖表）。

---

## 5. 活動區塊（/activities）規格 ── 含建置優先序

每個活動：可重試、難度分級、**失敗回饋用桃姨口氣給提示**（例：「莫急，閣看覓肥瘦的比例」），完成後跳**能力卡**。

**能力卡格式**（照 canon §7）：
```
【這題咧練的是 ⟨CT/認知能力⟩】
⟨一句白話說明這個能力是什麼⟩
⟨你剛剛做的哪個動作 = 用到這個能力⟩
```

**建置優先序（時間有限時由上往下做，P0 是繳交底線）：**

| 順位 | id | 練的能力 | 為什麼排這順位 |
|---|---|---|---|
| **P0** | `scheduleStoves`（多爐並進）★ | CT：平行化＋排程＋條件 | 全案招牌、正中「用時間控制」、滿足「CT demo」。**做一個長者玩得動的簡化版**：3 口爐、4–5 道菜、可拖放排時段、開桌時全部要熱。可重試＋提示。 |
| **P0** | `fridgeSearch`（揣冰櫥） | 認知：視覺短期記憶／注意 | 滿足「認知 demo」，且最好做（干擾物中找蠔油）。 |
| **P0** | `stepSort`（排步驟） | CT：序列 | 拖放排序，乾淨好做的第二個 CT。 |
| P1 | `condition`（條件 IF） | CT：條件判斷 | 三選一分支（蠔油+糖✅）。 |
| P1 | `memory`（點數） | 認知：工作記憶／更新 | 記各爐進度（**考阿明、不考桃姨**，絕不碰失智）。 |
| P2 | `pattern`（揣規律） | CT：模式辨別 | 找三道菜共同步驟＝爆香。 |
| P2 | `marketPick`（揀肉） | CT：抽象化＋模式 | 肥瘦三比七。 |
| P2 | `decompose`（拆場） | CT：問題分解 | 十工作分三階段。 |
| P3 | `menuRead`/`ingredient`（讀單／認食材） | 認知：命名 | 配對題，最輕。 |

> **底線**：做完 P0 三個就達標（≥2 數位活動 ＋ CT demo ＋ 認知 demo 都齊）。其餘往下加，越多自評表越漂亮。各活動正解、干擾項、資料全在 canon §7，直接用。

---

## 6. 視覺 / UX 鐵則（長者友善，解評語 B/D）

- **字級大**：旁白 ≥ 20px、對話框 ≥ 18px、按鈕文字 ≥ 18px；點擊區夠大（≥44px）。
- **高對比**：避免淺灰字；對話框底色與字夠對比。
- **簡背景強焦點**：UI 乾淨，一頁一焦點。
- **食物擬真、人臉插畫**（canon b2）：這是圖的事，但版面別讓 UI 蓋住食物特寫。
- **回饋明確**：對／錯都有清楚視覺＋桃姨一句話；錯不責備、給下一步提示。
- **無失憶/失智字眼**：任何 UI 文案都不准出現；記憶活動框成「鬥相共記」。

---

## 7. 圖檔接法（跟生圖表對齊，很重要）

- 圖檔命名 **`p01.png` … `p32.png`**，放在現有 repo 放故事圖的資料夾（`public/` 確定存在；子路徑多半是 `public/story/pages/`，**請 Code 先確認現況實際路徑、沿用之**，不要自行另開新路徑）。
- 老闆會用 Gemini 依「Gemini 生圖 prompt 表」生出 `p01–p32`，檔名一一對應。**Code 先用佔位圖（純色塊＋頁碼）把版面接起來**，圖到位後直接覆蓋同名檔即可，不必改程式。
- 比例：**所有頁同一個長寬比**（沿用現有圖的比例；若重來，用 3:2 橫式）。生圖表會請老闆統一輸出同比例。

---

## 8. 部署與安全網

- 老闆決定**直接在 main 上做**（main 現在是組員的期中版，不需保留）。動工前 Code **先 `git tag midterm-backup`（或一個備份 commit）** 把現狀標起來——git 歷史就留得住期中版，要回溯隨時可回，等於零風險地放手改。
- 重構期間 Vercel 會自動部署 WIP 版本，**沒關係**：繳交看的是 6/25 23:59 的狀態，只要**到死線時網址是可運作的完成版**即可。做完一輪先看 preview、確認沒問題。
- 最終正式部署 Vercel、**產出公開網址**（檢核表要交網址，記得確認分享權限「知道連結即可看」）。

---

## 9. 不要做 / 交給別條線的事

- **不要生圖**：插圖由老闆在 Gemini 跑（Code 只接檔名）。
- **不要錄影**：Demo 影片老闆親錄。
- **不要重寫劇情**：劇情、對白、活動正解都在 canon，照搬即可，不要自由發揮改故事。
- `.next`、`out` 是建置產物，不用手改。（先前的 `期末成品_辦桌版` 已移出 repo，不在此專案內。）
- 報告 Word / PPT / 繪本 PDF：另一條線（Chat）產，不在這個 repo 做。

---

## 10. 建議給 Code 的執行順序

1. 讀 canon + 本規格，盤點現有 repo 結構（路由、story 資料檔、圖檔路徑、audio 機制），回報現況與改動計畫。
2. 建架構：首頁兩入口、`/book`、`/activities` 骨架；把 story 資料檔改成辦桌 32 頁（先接佔位圖）。
3. 繪本翻頁＋對話框＋頁碼＋紅單翻面機關。
4. 活動 **P0 三個**（scheduleStoves／fridgeSearch／stepSort）＋能力卡。
5. preview 部署給老闆看 → 沒問題再往下加 P1/P2 活動、接真圖、正式部署。

> 每做完一個里程碑就停下回報，不要一口氣全做完才給看。
